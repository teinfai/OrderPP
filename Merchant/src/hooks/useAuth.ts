export const useAuth = () => {
  // TODO: replace with real auth state once API is wired up
  return {
    isAuthenticated: true,
    login: () => {
      /* no-op placeholder */
    },
    logout: () => {
      /* no-op placeholder */
    },
  };
};
